CLI tool to teleport to a file in a list of most recently modified files in a directory. The list is composed of all files recursively included within the specified directories and its subdirectories


USAGE:
//TODO
